//I WOULD LIKE TO MAKE ALL CONTROLLERS AS REST CONTROLLERS TO MAKE THINGS EASIER AND USE SAME FUNCTIONS ACROSS ALL CONTROLLERS IN FUTURE RELEASE
//BUT I HAVE NO TIME FOR TODAY SO TO POST A COMPARATOR YOU NEED TO MAKE IT WITH ONE LINE

/*
package madbrains.component; import madbrains.model.Element; class MyComparator implements Comparator<Element>{ int compare(Element a, Element b) {  return a.getValue().compareTo(b.getValue());  } }*/

/*
package madbrains.component

import java.util.Comparator;
import madbrains.catalogues.Element

class MyComparator implements Comparator<Element>{
    int compare(Element a, Element b) {
        return a.getValue().compareTo(b.getValue());
	foxy.stink();
    }
}
 */